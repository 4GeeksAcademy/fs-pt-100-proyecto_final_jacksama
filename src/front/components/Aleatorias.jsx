import { Link } from "react-router-dom";
import '../CSS/Aleatorias.css';

export const Aleatorias = () => {
        // const [favoritos, setFavoritos] = useState([]);

        // const toggleFavorito = (texto) => {
        //     setFavoritos((prev) =>
        //         prev.includes(texto) ? prev.filter((f) => f !== texto) : [...prev, texto]
        //     );
        // };

    //     return <div className="aleatorias-container">
    //         <h2 className="aleatorias-title">Frases Aleatorias</h2>
    //         <p className="aleatorias-description">Aquí encontrarás frases aleatorias para inspirarte.</p>
    //         <div className="aleatorias-frases">
    //             {frases.map((frase, index) => (
    //                 <div key={index} className="aleatorias-frase">
    //                     <p>{frase}</p>
    //                     <button onClick={() => toggleFavorito(frase)}>
    //                         {favoritos.includes(frase) ? "Quitar de Favoritos" : "Agregar a Favoritos"}
    //                     </button>
    //                 </div>
    //             ))}
    //         </div>
    //     </div>;
    };
